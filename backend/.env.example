# Database configuration (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@db:5432/legal_metrology

# JWT Security
JWT_SECRET=8e9282fa2c8f8b3b428d08c5c70ea91b2db97561858c2a8684bd30a10f92b7ef
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Application storage paths
UPLOAD_DIR=uploads
REPORT_DIR=reports

# CORS Allowed Origins (Comma-separated list or ["*"])
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000"]

# Service Providers: 'paddleocr' or 'mock'
OCR_PROVIDER=mock

# Storage Provider: 'local' (AWS S3 placeholder ready)
STORAGE_PROVIDER=local
